<%= form_for(@parking_ticket) do |f| %>
  <% if @parking_ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parking_ticket.errors.count, "error") %> prohibited this parking_ticket from being saved:</h2>

      <ul>
      <% @parking_ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fee %><br>
    <%= f.text_field :fee %>
  </div>
  <div class="field">
    <%= f.label :reporter %><br>
    <%= f.text_field :reporter %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :student_id %><br>
    <%= f.text_field :student_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
